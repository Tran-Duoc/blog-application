api:
  version:
    prefix: api/v1
server:
  port: 8088
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/blog_app
    username: root
    password: td1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
